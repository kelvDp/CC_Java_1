package abstractAndInterfaces;

public interface INTFclass {
    
    // dont' have to use the abstract keyword here cause an interface is implicitly abstract
    public void print();  
    // also only name it here and initialize in subclass.
}
